if (typeof document !== "undefined") {
  const planetDropTranslations = {
    en: {
      pageTitle: "Planet Drop | Liu Chuan Feng",
      pageDescription: "Planet Drop is a physics-based merge puzzle with roguelike upgrades, created independently by Liu Chuan Feng.",
      skip: "Skip to content",
      portfolioHomeLabel: "Back to Liu Chuan Feng's portfolio",
      languageLabel: "Language selection",
      backToWorkShort: "Portfolio",
      heroEyebrow: "A universe formed by merging",
      heroTagline: "Drop small worlds. Merge a universe.",
      heroDescription: "A physics-based merge puzzle where every drop reshapes the board—and every roguelike upgrade changes the strategy.",
      availableOn: "Available on",
      downloadOn: "Download on the",
      downloadLabel: "Download Planet Drop",
      heroScreenshotAlt: "Planet Drop gameplay showing colorful planets merging",
      dropKicker: "Core gameplay",
      dropTitle: "Every drop changes the board.",
      dropIntro: "Aim, release, and let physics take over. Space is limited, so one small decision can open a chain—or end the run.",
      dropCardTitle: "Drop with intent",
      dropCardText: "Position the next planet, anticipate its bounce, and make room before the container overflows.",
      mergeCardTitle: "Match and merge",
      mergeCardText: "When two identical planets meet, they combine into a larger world and create new scoring possibilities.",
      chainCardTitle: "Build the chain",
      chainCardText: "Read the board a few moves ahead and turn a difficult layout into a satisfying cascade of merges.",
      mergeKicker: "Gameplay",
      mergeTitle: "Watch a universe take shape.",
      mergeIntro: "Simple input meets lively 2D physics. The rules are immediate; the board that emerges is different every time.",
      gameplayVideoTitle: "Planet Drop gameplay video",
      screenshot1Alt: "Planet Drop start screen",
      screenshot2Alt: "Planets merging into larger worlds",
      screenshot3Alt: "Roguelike upgrade choices",
      screenshot4Alt: "Planet Drop score and stardust reward screen",
      previousScreenshot: "Previous screenshot",
      nextScreenshot: "Next screenshot",
      chooseKicker: "Roguelike upgrades",
      chooseTitle: "Choose a rule. Change the run.",
      chooseIntro: "Upgrades alter size, score, gravity, and even planet behavior. Combining them turns a familiar merge puzzle into a new build each time.",
      upgradeSizeTitle: "Smaller worlds",
      upgradeSizeText: "Shrink upcoming planets—or the entire board—to recover valuable space.",
      upgradeScoreTitle: "Score multipliers",
      upgradeScoreText: "Reward careful planning with stronger score growth and powerful combinations.",
      upgradeGravityTitle: "Altered gravity",
      upgradeGravityText: "Change how the board settles and discover new ways to connect distant planets.",
      upgradeRainbowTitle: "Special effects",
      upgradeRainbowText: "Create rainbow planets and other effects that can rescue a run—or set up a spectacular chain.",
      upgradeScreenshotAlt: "Three Planet Drop roguelike upgrade cards",
      evolveKicker: "Independent development",
      evolveTitle: "One developer, from first idea to release.",
      evolveText1: "Planet Drop was independently designed, programmed, tested, submitted, and released by Liu Chuan Feng.",
      evolveText2: "The project brings together responsive physics, a flexible upgrade system, mobile UI, store deployment, and the many small decisions required to turn a prototype into a published game.",
      soloItem1: "Game design & system planning",
      soloItem2: "Unity / C# implementation",
      soloItem3: "UI, tuning & release operations",
      gameIconAlt: "Planet Drop game icon",
      finalKicker: "Begin the next run",
      finalTitle: "How large can your universe become?",
      finalText: "Drop a planet, choose your upgrades, and build a strategy that only exists in this run.",
      backToWork: "Back to personal projects",
      footerText: "An independently developed merge puzzle."
    },
    "zh-TW": {
      pageTitle: "Planet Drop 遊戲專頁 | 劉川楓",
      pageDescription: "Planet Drop 是由劉川楓獨立開發、結合物理合成與 Roguelike 強化的益智遊戲。",
      skip: "跳至主要內容",
      portfolioHomeLabel: "返回劉川楓個人網站",
      languageLabel: "語言選擇",
      backToWorkShort: "個人網站",
      heroEyebrow: "由合成誕生的宇宙",
      heroTagline: "落下小小星球，合成你的宇宙。",
      heroDescription: "每一次投放都會重塑盤面、每一次 Roguelike 強化都會改變策略的物理合成益智遊戲。",
      availableOn: "前往下載",
      downloadOn: "前往下載",
      downloadLabel: "下載 Planet Drop",
      heroScreenshotAlt: "Planet Drop 遊戲中彩色行星合成的畫面",
      dropKicker: "核心玩法",
      dropTitle: "每一次投放，都會改變盤面。",
      dropIntro: "瞄準、放手，接著交給物理運算。空間有限，一個看似微小的決定可能開啟連鎖，也可能讓本局結束。",
      dropCardTitle: "思考後再投放",
      dropCardText: "安排下一顆行星的位置、預判彈跳，並在容器滿出之前創造空間。",
      mergeCardTitle: "相同星球合成",
      mergeCardText: "兩顆相同行星相遇後，會合成為更大的世界，同時打開新的得分可能。",
      chainCardTitle: "創造連鎖反應",
      chainCardText: "提前閱讀幾步盤面，將難解的排列轉化成一連串爽快的合成。",
      mergeKicker: "Gameplay",
      mergeTitle: "看著一個宇宙逐步成形。",
      mergeIntro: "簡單操作結合生動的 2D 物理。規則一看就懂，但每一局形成的盤面都不相同。",
      gameplayVideoTitle: "Planet Drop 遊戲玩法影片",
      screenshot1Alt: "Planet Drop 開始畫面",
      screenshot2Alt: "行星合成為更大世界的遊戲畫面",
      screenshot3Alt: "Roguelike 強化選擇畫面",
      screenshot4Alt: "Planet Drop 分數與星塵獎勵畫面",
      previousScreenshot: "上一張遊戲截圖",
      nextScreenshot: "下一張遊戲截圖",
      chooseKicker: "Roguelike 強化",
      chooseTitle: "選擇一條規則，改變整場遊戲。",
      chooseIntro: "強化會改變尺寸、分數、重力，甚至行星本身的行為。不同組合讓熟悉的合成玩法，每一局都形成新的流派。",
      upgradeSizeTitle: "縮小行星",
      upgradeSizeText: "縮小接下來出現的行星，或讓整個盤面的星球變小，重新取得寶貴空間。",
      upgradeScoreTitle: "分數倍率",
      upgradeScoreText: "讓謹慎規劃獲得更高回報，並組合出更強力的得分效果。",
      upgradeGravityTitle: "改變重力",
      upgradeGravityText: "改變盤面落定的方式，發現讓遠處行星彼此連接的新策略。",
      upgradeRainbowTitle: "特殊效果",
      upgradeRainbowText: "創造彩虹行星與其他特殊效果，在危急時救回盤面，或準備一次華麗連鎖。",
      upgradeScreenshotAlt: "三張 Planet Drop Roguelike 強化卡片",
      evolveKicker: "獨立開發",
      evolveTitle: "從第一個想法到正式上架，由一人完成。",
      evolveText1: "Planet Drop 由劉川楓獨立負責設計、程式開發、測試、商店送審與正式發行。",
      evolveText2: "這個專案結合流暢的物理操作、可擴充的強化系統、行動裝置 UI 與雙平台上架流程，也包含把原型磨成一款完整遊戲所需的無數細節。",
      soloItem1: "遊戲設計與系統規劃",
      soloItem2: "Unity / C# 程式實作",
      soloItem3: "UI、數值調整與上架營運",
      gameIconAlt: "Planet Drop 遊戲圖示",
      finalKicker: "開始下一局",
      finalTitle: "你的宇宙，能成長到多大？",
      finalText: "投下一顆行星、選擇本局強化，組合出只屬於這一次挑戰的策略。",
      backToWork: "返回個人網站作品區",
      footerText: "由一人獨立開發的星球合成益智遊戲。"
    },
    ja: {
      pageTitle: "Planet Drop ゲーム紹介 | 劉川楓",
      pageDescription: "Planet Dropは、物理マージとローグライク強化を組み合わせた、劉川楓による個人開発パズルゲームです。",
      skip: "メインコンテンツへ移動",
      portfolioHomeLabel: "劉川楓のポートフォリオへ戻る",
      languageLabel: "言語選択",
      backToWorkShort: "ポートフォリオ",
      heroEyebrow: "マージから生まれる宇宙",
      heroTagline: "小さな惑星を落とし、自分だけの宇宙をつくる。",
      heroDescription: "一手ごとに盤面が変わり、ローグライク強化の選択によって戦略も変化する物理マージパズルです。",
      availableOn: "ダウンロード",
      downloadOn: "ダウンロード",
      downloadLabel: "Planet Dropをダウンロード",
      heroScreenshotAlt: "カラフルな惑星が合体するPlanet Dropのゲーム画面",
      dropKicker: "コアゲームプレイ",
      dropTitle: "一手ごとに、盤面が変わる。",
      dropIntro: "狙いを定めて、惑星を落とし、あとは物理演算に任せる。限られた空間では、小さな判断が連鎖を生み、ときにはゲーム終了につながります。",
      dropCardTitle: "狙って落とす",
      dropCardText: "次の惑星の位置と跳ね方を予測し、容器からあふれる前にスペースをつくります。",
      mergeCardTitle: "同じ惑星をマージ",
      mergeCardText: "同じ惑星が触れると、より大きな世界へと合体し、新しい得点チャンスが生まれます。",
      chainCardTitle: "連鎖を組み立てる",
      chainCardText: "数手先まで盤面を読み、難しい配置を気持ちのよいマージの連鎖へ変えていきます。",
      mergeKicker: "ゲームプレイ",
      mergeTitle: "宇宙が形になる瞬間を。",
      mergeIntro: "シンプルな操作と生き生きとした2D物理。ルールはすぐに理解できても、できあがる盤面は毎回異なります。",
      gameplayVideoTitle: "Planet Drop ゲームプレイ動画",
      screenshot1Alt: "Planet Dropのスタート画面",
      screenshot2Alt: "惑星がより大きな世界へ合体するゲーム画面",
      screenshot3Alt: "ローグライク強化の選択画面",
      screenshot4Alt: "Planet Dropのスコアとスターダスト報酬画面",
      previousScreenshot: "前のゲーム画面",
      nextScreenshot: "次のゲーム画面",
      chooseKicker: "ローグライク強化",
      chooseTitle: "ルールを選び、ランを変える。",
      chooseIntro: "サイズ、スコア、重力、さらには惑星の挙動まで変える強化。組み合わせ次第で、同じマージパズルから毎回違うビルドが生まれます。",
      upgradeSizeTitle: "惑星を小さく",
      upgradeSizeText: "これから出る惑星や盤面全体を小さくし、貴重なスペースを取り戻します。",
      upgradeScoreTitle: "スコア倍率",
      upgradeScoreText: "丁寧な組み立てを大きな得点へ変え、強力な組み合わせをつくります。",
      upgradeGravityTitle: "重力を変える",
      upgradeGravityText: "盤面の落ち着き方を変え、離れた惑星をつなぐ新しい方法を見つけます。",
      upgradeRainbowTitle: "特殊効果",
      upgradeRainbowText: "レインボー惑星などの効果でピンチを救い、鮮やかな連鎖を準備します。",
      upgradeScreenshotAlt: "3枚のPlanet Dropローグライク強化カード",
      evolveKicker: "個人開発",
      evolveTitle: "最初のアイデアからリリースまで、一人で。",
      evolveText1: "Planet Dropは、劉川楓が企画、プログラミング、テスト、ストア申請、リリースまで一貫して手がけた個人開発作品です。",
      evolveText2: "反応のよい物理挙動、拡張しやすい強化システム、モバイルUI、両ストアへの配信。そして、プロトタイプを完成したゲームへ磨き上げるための細かな判断を一つの作品にまとめました。",
      soloItem1: "ゲームデザイン・システム設計",
      soloItem2: "Unity / C# 実装",
      soloItem3: "UI・調整・リリース運用",
      gameIconAlt: "Planet Dropのゲームアイコン",
      finalKicker: "次のランを始めよう",
      finalTitle: "あなたの宇宙は、どこまで大きくなる？",
      finalText: "惑星を落とし、強化を選び、このランだけの戦略を組み立てよう。",
      backToWork: "ポートフォリオの作品一覧へ戻る",
      footerText: "個人開発の惑星マージパズル。"
    }
  };

  const supportedLanguages = ["en", "zh-TW", "ja"];
  const motionPreference = window.matchMedia("(prefers-reduced-motion: reduce)");

  function normalizeLanguage(language) {
    const value = (language || "").toLowerCase();
    if (value.startsWith("zh")) return "zh-TW";
    if (value.startsWith("ja")) return "ja";
    return "en";
  }

  function getInitialLanguage() {
    try {
      const saved = window.localStorage.getItem("portfolio-language");
      if (saved && supportedLanguages.includes(saved)) return saved;
    } catch {
      // Browser language remains a safe fallback when storage is unavailable.
    }

    return normalizeLanguage(navigator.languages?.[0] || navigator.language);
  }

  function setLanguage(language) {
    const selected = supportedLanguages.includes(language) ? language : "en";
    const dictionary = planetDropTranslations[selected];
    const page = document.querySelector(".planet-drop-page");
    if (!page) return;

    document.documentElement.lang = selected;
    document.title = dictionary.pageTitle;

    const description = document.querySelector('meta[name="description"]');
    if (description) description.setAttribute("content", dictionary.pageDescription);

    page.querySelectorAll("[data-i18n]").forEach((element) => {
      const key = element.dataset.i18n;
      if (dictionary[key]) element.textContent = dictionary[key];
    });

    page.querySelectorAll("[data-i18n-alt]").forEach((element) => {
      const key = element.dataset.i18nAlt;
      if (dictionary[key]) element.alt = dictionary[key];
    });

    page.querySelectorAll("[data-i18n-title]").forEach((element) => {
      const key = element.dataset.i18nTitle;
      if (dictionary[key]) element.title = dictionary[key];
    });

    page.querySelectorAll("[data-i18n-aria]").forEach((element) => {
      const key = element.dataset.i18nAria;
      if (dictionary[key]) element.setAttribute("aria-label", dictionary[key]);
    });

    page.querySelectorAll("[data-lang]").forEach((button) => {
      const active = button.dataset.lang === selected;
      button.classList.toggle("active", active);
      button.setAttribute("aria-pressed", String(active));
    });

    try {
      window.localStorage.setItem("portfolio-language", selected);
    } catch {
      // The page remains fully functional without persistent storage.
    }
  }

  function initializeHero(page) {
    if (motionPreference.matches) return;

    let alreadySeen = false;
    try {
      alreadySeen = window.sessionStorage.getItem("planet-drop-hero-seen") === "true";
      if (!alreadySeen) window.sessionStorage.setItem("planet-drop-hero-seen", "true");
    } catch {
      // If session storage is unavailable, playing once on this load is harmless.
    }

    if (alreadySeen) return;
    document.documentElement.classList.add("pd-hero-intro");
    window.setTimeout(() => document.documentElement.classList.remove("pd-hero-intro"), 1400);
  }

  function initializeReveal() {
    const items = Array.from(document.querySelectorAll(".planet-drop-page .pd-reveal"));
    if (!items.length) return;

    if (motionPreference.matches || !("IntersectionObserver" in window)) {
      items.forEach((item) => item.classList.add("is-visible"));
      return;
    }

    try {
      const observer = new IntersectionObserver((entries, revealObserver) => {
        entries.forEach((entry) => {
          if (!entry.isIntersecting) return;
          entry.target.classList.add("is-visible");
          revealObserver.unobserve(entry.target);
        });
      }, { threshold: 0.12, rootMargin: "0px 0px -7% 0px" });

      document.documentElement.classList.add("pd-motion-ready");
      items.forEach((item) => observer.observe(item));
    } catch {
      document.documentElement.classList.remove("pd-motion-ready");
      items.forEach((item) => item.classList.add("is-visible"));
    }
  }

  function initializeCarousel(page) {
    const carousel = page.querySelector("[data-carousel]");
    if (!carousel) return;

    const slides = Array.from(carousel.querySelectorAll("[data-slide]"));
    const dots = Array.from(carousel.querySelectorAll(".pd-carousel-dots span"));
    const previous = carousel.querySelector("[data-carousel-previous]");
    const next = carousel.querySelector("[data-carousel-next]");
    if (!slides.length || !previous || !next) return;

    let current = 0;
    carousel.classList.add("pd-js-carousel");

    const show = (index) => {
      current = (index + slides.length) % slides.length;
      slides.forEach((slide, slideIndex) => slide.classList.toggle("is-current", slideIndex === current));
      dots.forEach((dot, dotIndex) => dot.classList.toggle("is-current", dotIndex === current));
    };

    previous.addEventListener("click", () => show(current - 1));
    next.addEventListener("click", () => show(current + 1));
    carousel.addEventListener("keydown", (event) => {
      if (event.key === "ArrowLeft") show(current - 1);
      if (event.key === "ArrowRight") show(current + 1);
    });
    show(0);
  }

  function initializeOrbitNavigation(page) {
    if (!("IntersectionObserver" in window)) return;

    const links = new Map(
      Array.from(page.querySelectorAll("[data-orbit-link]")).map((link) => [link.dataset.orbitLink, link])
    );
    const sections = Array.from(page.querySelectorAll("[data-orbit-section]"));
    if (!links.size || !sections.length) return;

    const setActive = (id) => {
      links.forEach((link, linkId) => {
        if (linkId === id) link.setAttribute("aria-current", "true");
        else link.removeAttribute("aria-current");
      });
    };

    const observer = new IntersectionObserver((entries) => {
      const visible = entries
        .filter((entry) => entry.isIntersecting)
        .sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];
      if (visible) setActive(visible.target.dataset.orbitSection);
    }, { rootMargin: "-28% 0px -56% 0px", threshold: [0, 0.1, 0.35] });

    sections.forEach((section) => observer.observe(section));
    setActive("drop");
  }

  function initializeParallax(page) {
    if (motionPreference.matches) return;

    let scheduled = false;
    const update = () => {
      scheduled = false;
      const speed = window.innerWidth < 680 ? 0.055 : 0.075;
      const offset = Math.min(window.scrollY * speed, 54);
      page.style.setProperty("--pd-parallax-y", `${offset.toFixed(2)}px`);
    };

    const onScroll = () => {
      if (scheduled) return;
      scheduled = true;
      window.requestAnimationFrame(update);
    };

    window.addEventListener("scroll", onScroll, { passive: true });
    update();
  }

  function initializePlanetDrop() {
    const page = document.querySelector(".planet-drop-page");
    if (!page || page.dataset.initialized === "true") return;
    page.dataset.initialized = "true";

    page.querySelectorAll("[data-lang]").forEach((button) => {
      button.addEventListener("click", () => setLanguage(button.dataset.lang));
    });

    setLanguage(getInitialLanguage());
    initializeHero(page);
    initializeReveal();
    initializeCarousel(page);
    initializeOrbitNavigation(page);
    initializeParallax(page);

    motionPreference.addEventListener?.("change", (event) => {
      if (!event.matches) return;
      document.documentElement.classList.remove("pd-hero-intro");
      page.style.setProperty("--pd-parallax-y", "0px");
      page.querySelectorAll(".pd-reveal").forEach((item) => item.classList.add("is-visible"));
    });
  }

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", initializePlanetDrop, { once: true });
  } else {
    initializePlanetDrop();
  }
}
